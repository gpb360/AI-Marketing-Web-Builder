import { RegisterForm } from '@/components/auth/RegisterForm';

export default function RegisterPage() {
  return <RegisterForm />;
}

export const metadata = {
  title: 'Create Account | AI Marketing Web Builder',
  description: 'Create your AI Marketing Web Builder account',
};