import { LoginForm } from '@/components/auth/LoginForm';

export default function LoginPage() {
  return <LoginForm />;
}

export const metadata = {
  title: 'Sign In | AI Marketing Web Builder',
  description: 'Sign in to your AI Marketing Web Builder account',
};