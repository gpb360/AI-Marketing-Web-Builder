'use client';

import React from 'react';
import { VisualBuilder } from '@/components/builder/VisualBuilder';

export default function BuilderPage() {
  return <VisualBuilder className="h-screen" />;
}