# GitHub Actions are intentionally disabled for this repository
# This file prevents any workflows from running accidentally

name: Actions Disabled

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Actions Disabled
        run: |
          echo "GitHub Actions are disabled for this repository"
          echo "This workflow exists only to prevent other workflows from running"
          exit 0